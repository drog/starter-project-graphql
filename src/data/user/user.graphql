type User {
  id: String!
  name: String
  email: String
  img: String
  role: String
  google: String
  
}

type Query {
  users: [User]
  user(id: String!): User
}

type Mutation {
  updateUser(id: String!, name: String!): User
}